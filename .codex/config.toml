# Codex CLI project config.
# All invocations go through .claude/scripts/codex_handoff.py, which sets the
# sandbox per phase (read-only for plan/review, workspace-write for implement).
# Model and reasoning effort are selected by the runner (CLI/env/risk-tier
# precedence); see .claude/docs/CODEX_TASK_CONTRACT.md.
sandbox_mode = "read-only"
approval_policy = "never"
